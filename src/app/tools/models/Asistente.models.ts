export class Asistente {

    name: string
    typeDocument: string
    document: string
    phone: number
    email: string

    constructor(name: string, typeDocument: string, document: string, phone: number, email: string) {
        this.name = name
        this.typeDocument = typeDocument
        this.document = document
        this.phone = phone
        this.email = email
    }

}